---
name: init
description: Initialize Cuff at the current Git worktree root through the public CLI.
---

Run exactly one `cuff init --json` from the requested Git worktree root. Return
the JSON result. Do not create, edit, migrate, or remove `.fab7/cuff` paths directly.
