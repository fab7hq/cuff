---
name: init
description: Initialize Fab7 at the current Git worktree root through the public CLI.
---

Run exactly one `fab7 init --json` from the requested Git worktree root. Return
the JSON result. Do not create, edit, migrate, or remove `.fab7` paths directly.
